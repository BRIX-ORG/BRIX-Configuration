apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "image-service.fullname" . }}
  labels:
    {{- include "image-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
