apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "be.fullname" . }}
  labels:
    {{- include "be.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
