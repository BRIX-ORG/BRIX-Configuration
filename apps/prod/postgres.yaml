# ============================================
# ArgoCD Application - PostgreSQL (PROD)
# Uses Bitnami Helm chart
# ============================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: brix-postgres-prod
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: brix
    environment: prod
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: brix-prod
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: postgresql
    targetRevision: 16.*.*
    helm:
      releaseName: brix-postgres
      valueFiles: []
      values: |
        {{- .Files.Get "../../environments/prod/postgres-values.yaml" }}
  destination:
    server: https://kubernetes.default.svc
    namespace: brix-prod
  syncPolicy:
    # -- NEVER auto-prune database in prod
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
